# Model Card

No statistical, neural, language, embedding, or retrieval model is used.

The module is a deterministic Rust rule engine with fixed normalization,
integer scoring, thresholds, and tie-breaking. It performs no training,
inference, network access, or online learning.
